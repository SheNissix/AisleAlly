<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:paddingTop="16dp"
    android:paddingBottom="100dp"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView style="@style/PageTitle" android:text="Items"/>

        <!-- ITEMS LIST CARD -->
        <LinearLayout style="@style/Card" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Items List"/>
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_items"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"/>
        </LinearLayout>

        <!-- ADD/EDIT ITEM CARD -->
        <LinearLayout style="@style/Card" android:orientation="vertical">
            <TextView style="@style/SectionTitle" android:text="Add / Edit Item"/>

            <Spinner android:id="@+id/spinner_category"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:background="@drawable/input_bg"/>

            <EditText android:id="@+id/et_item_name"    style="@style/InputField" android:hint="Item Name"/>
            <EditText android:id="@+id/et_item_price"   style="@style/InputField" android:hint="Price (₱)" android:inputType="numberDecimal"/>
            <EditText android:id="@+id/et_item_calories" style="@style/InputField" android:hint="Calories" android:inputType="numberDecimal"/>
            <EditText android:id="@+id/et_item_protein" style="@style/InputField" android:hint="Protein (g)" android:inputType="numberDecimal"/>
            <EditText android:id="@+id/et_item_fiber"   style="@style/InputField" android:hint="Fiber (g)" android:inputType="numberDecimal"/>
            <EditText android:id="@+id/et_item_fat"     style="@style/InputField" android:hint="Fat (g)" android:inputType="numberDecimal"/>

            <Button android:id="@+id/btn_save_item"
                style="@style/PrimaryButton"
                android:text="Save Item"/>
        </LinearLayout>

    </LinearLayout>
</ScrollView>
